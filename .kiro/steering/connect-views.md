---
inclusion: manual
last_refreshed: 2026-09-10
entry_count: 66
source_urls:
  - https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/overview--page
  - https://docs.aws.amazon.com/connect/latest/adminguide/step-by-step-guided-experiences.html
content_checksum: sha256:1832be115dede9e8
---

# Connect view dictionary catalog

Quick lookup table for every component, view template, and view-related
docs page in the public Amazon Connect View Dictionary, plus a curated
index of admin-guide topic pages that explain how Views and step-by-step
guides hang together at the flow / permissions / runtime layer. Both
sections are generated by
``uv run python .kiro/scripts/refresh_connect_views.py`` from two upstream
sources:

- the View Dictionary Storybook bundle (CloudFront, JS-rendered) — for
  the four component catalog tables.
- the Amazon Connect admin guide (``docs.aws.amazon.com/connect``,
  served as ``.md`` alongside ``.html``) — for the **Admin guide
  pages** group below.

**Activation:** this file is opt-in. Reference it from chat with
`#connect-views` when you're authoring a customer-managed view template,
picking a UI component for a `Show view` block, or wiring a form into
a flow.

**Freshness rule (for the agent):** before relying on this catalog,
compare the `last_refreshed` date in the front matter with today's
date. If the file is older than 7 days, run
``uv run python .kiro/scripts/refresh_connect_views.py`` to refresh it
before answering. Always refresh when the user explicitly asks for it.
A regenerated file with no diff is fine — `last_refreshed` is the only
thing that needs updating, and the script handles that automatically.

For depth on any entry (full props table with descriptions, defaults,
examples, schema), call the `get_view_component_doc` MCP tool with the
component's story slug — it's the bit after `?path=/docs/` in the
component link, e.g. `ui-component-datepicker--with-all`. The tool
drives a headless Chromium against the View Dictionary Storybook and
returns structured props.

For depth on any **Admin guide** row, swap the link's `.html` for `.md`
(or open the rendered page) — the admin guide is plain markdown so a
direct fetch is enough; no browser needed.

For the higher-level view APIs (`CreateView`, `UpdateView`, …) see the
[Amazon Connect View resource APIs](https://docs.aws.amazon.com/connect/latest/APIReference/views-api.html).


## AWS-managed Views

Pre-built end-to-end view templates fully managed by Amazon Connect. Use these as a `Show view` block target when you want a ready-made screen instead of composing one from UI components. _Common Configuration_ is a reference page describing fields shared by every AWS-managed template.

| Component | Description |
| --- | --- |
| [Cards](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/aws-managed-views-cards--with-all) | This view lets you guide your agent by presenting them with a list of Topics to choose from as soon as they accept the contact. This UX is based on Amazon’s “Listen, Match, Solve” pattern that we use in our own contact centers. |
| [Common Configuration](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/aws-managed-views-common-configuration--with-all) | _(no description in source)_ |
| [Confirmation](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/aws-managed-views-confirmation--with-all) | This view is a page to show users after a form has been submitted or an action has been completed. In this pre-built template you can provide a summary of what has happened, any next steps, and prompts. The Confirmation view supports the persistent attribute bar, an icon/image, Headline and sub-headline, along with a back to home navigation button. |
| [Detail](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/aws-managed-views-detail--with-all) | This view lets you display information to the agent and provide them with a list of actions that they can take as next steps. It also optionally supports the standard back navigation and persistent attribute bar. |
| [Form](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/aws-managed-views-form--with-all) | With this view you can provide the agent with input fields to gather required data and submit data to backend systems. This view consists of multiple “Sections” with a predefined “Section” style with header and the body consists of various Input fields arranged in an Column or a grid layout format as shown below. |
| [List](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/aws-managed-views-list--with-all) | With this view you can display information to the agent as a list of items with titles and descriptions. Items may also act as links, with actions attached. It also optionally supports the standard back navigation and persistent attribute header. |


## Customer-managed Views

Custom view resources that you author and version yourself with the Connect view APIs. Useful when AWS-managed templates do not fit and you need full control over the JSON template.

| Component | Description |
| --- | --- |
| [Customer-managed Views](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/customer-managed-views-customer-managed-views--page) | _(no description in source)_ |


## UI Components

Connect-managed UI elements that you compose inside a customer-managed view template's `Body` field. Default styles match the Connect look and feel; most props can be overridden when the view is configured in a flow. _UI Components_ is the umbrella reference page covering shared structure and props.

| Component | Description |
| --- | --- |
| [alert](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-alert--default-case) | Alert messages can be used to notify the user about something special: error, success, information or warning. |
| [Application](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-application--with-agent-workspace-example) | A component used for agents to open third-party applications (only available to agents using workspace). For app name use the namespace from the AWS console. Below is a snapshot of the agent workspace with the Wikipedia application rendered using this component. |
| [AttributeBar](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-attributebar--with-attributes) | Display essential information, such as customer names or contact queues, in a horizontal bar typically positioned on the top of the view. |
| [AttributeSection](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-attributesection--with-all) | A dedicated section for displaying key attributes—such as contact details, customer profiles, case data, or third-party information—in a clear and structured layout. |
| [button](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-button--default-case) | An interactive component that can be activated by a user. Once activated, it then performs an action, such as submitting a form or opening a dialog. |
| [ButtonGroup](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-buttongroup--with-all) | A component that contains multiple button components, allowing agents or end customers to select an action (i.e., flow branch) to go to the next view in a guide. |
| [card](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-card--with-all) | A component containing content and actions about a single topic. |
| [CheckboxGroup](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-checkboxgroup--with-all) | Component used to group together a set of checkbox buttons. |
| [container](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-container--default-case) | Defines the section of a view. Other UI components can be placed inside of containers to make it easier to organize and maintain the view’s layout. |
| [DatePicker](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-datepicker--with-all) | Component that lets the user pick a date by either typing it into the input field or selecting it from a calendar. |
| [Detail](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-detail--with-all) | A component designed to display information in a diverse set of formats, including strings, embedded HTML, and a list of key-value pairs with its corresponding header. |
| [Dropdown](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-dropdown--with-all) | Component that lets the user select from a list options. This component can be configured to force the user to select only one option or allow the user to select multiple options. |
| [expandableSection](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-expandablesection--default-case) | A type of container that includes the ability to hide and shoe the content. |
| [FormInput](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-forminput--with-all) | Input field that a user can enter text or numbers into. |
| [header](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-header--default-case) | Title or subtitle you want to display in the view |
| [HTMLBox](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-htmlbox--with-template-string) | A text box component that accepts HTML template strings in JSX format to render content in custom styles. |
| [Icon](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-icon--default-case) | Graphical image that can be used in a view |
| [Image](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-image--with-all) | Graphical image that can be used in a view |
| [link](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-link--default-case) | A hyperlink component that lets the user open up an external website in a browser tab or a case in agent workspace tab. |
| [loader](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-loader--default-case) | An animation that alerts the *user that the view is loading |
| [RadioGroup](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-radiogroup--with-all) | Component that lets the user choose only one option from a predefined set of options. |
| [Section](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-section--with-all) | A wrapper component with a heading that holds other UI components to simplify layout organization and maintenance of a view. |
| [Table](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-table--default) | A data table component that displays structured data in rows and columns with support for filtering, pagination, and actions (i.e., branches from the ShowView block). |
| [Textarea](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-textarea--with-all) | Input field that lets user enter multiple lines of text. |
| [TextBox](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-textbox--default) | A read-only component that presents text to agents or end customers in paragraph format. |
| [TimePicker](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-timepicker--with-all) | Component that lets the user pick a time by either typing it into the input field or selecting it from a dropdown. |
| [Toggle](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-toggle--with-all) | Component that lets user choose between two different options. |
| [UI Components](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/ui-component-ui-components--page) | _(no description in source)_ |


## FormView Components

Subset of UI components that are valid inside a `Form` UI component. They render input fields whose values are submitted via the `SubmitButton` and surfaced on the `Show view` block's branches.

| Component | Description |
| --- | --- |
| [CheckboxGroup](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-checkboxgroup--with-all) | Component used to group together a set of checkbox buttons. This component us issuable in the Form view. |
| [DatePicker](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-datepicker--with-all) | Component that lets the user pick a date by either typing it into the input field or selecting it from a calendar. |
| [Dropdown](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-dropdown--with-all) | Component that lets the user select from a list options. This component can be configured to force the user to select only one option or allow the user to select multiple options. |
| [FormInput](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-forminput--with-all) | Input field that a user can enter text or numbers into. |
| [RadioGroup](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-radiogroup--with-all) | Component that lets the user choose only one option from a predefined set of options. |
| [SubmitButton](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-submitbutton--with-all) | A button that submits all form data inside of a form component, clicking the button will move the Guide to the next view through a branch in the ShowView block and send data to the views namespace to be saved on the contact or 1st/3rd party applications via Flows |
| [TextArea](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-textarea--with-all) | Input field that lets user enter multiple lines of text. |
| [TimePicker](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-timepicker--with-all) | Component that lets the user pick a time by either typing it into the input field or selecting it from a dropdown. |
| [Toggle](https://d3irlmavjxd3d8.cloudfront.net/?path=/docs/formview-component-toggle--with-all) | Component that lets user choose between two different options. |


## Admin guide pages

Topic-level pages from the Amazon Connect admin guide that explain how
views, step-by-step guides, and the no-code UI builder hang together.
Use these when the Storybook component reference is not enough — for
example, when you need permissions setup, the flow patterns that drive
a view, or the runtime data plumbing.

URLs link to the rendered HTML page; swap `.html` → `.md` to fetch the
markdown source (the same source this catalog is generated from).

### Setup & permissions

| Topic | Description |
| --- | --- |
| [Step-by-step guides — overview](https://docs.aws.amazon.com/connect/latest/adminguide/step-by-step-guided-experiences.html) | Umbrella page: build screen pops and multi-page agent guides by combining a flow, the Show view block, and one or more Views. Covers the runtime model where the guide runs as its own background chat contact, and complex JSON pass-through between the workspace and flows. |
| [Enable step-by-step guides](https://docs.aws.amazon.com/connect/latest/adminguide/enable-guided-experiences-sg.html) | Permissions checklist: grant `Channels and flows - Views` plus `Flows - Edit, Create` to authors, `Agent Applications - Custom views` to agents, and bump the concurrent-active-chats service quota since each running guide spawns a chat contact. |
| [Smart default ACW guide](https://docs.aws.amazon.com/connect/latest/adminguide/enable-smart-default-guides-acw.html) | New instances ship with a default after-contact-work guide; for older instances, import the provided flow JSON, point its Show view block at the AWS-managed `after contact work` view, and wire it in via a `Set event flow` block. |
| [Customize the agent workspace theme](https://docs.aws.amazon.com/connect/latest/adminguide/customize-theme-agent-workspace.html) | Branding the agent workspace shell (logo, fonts, colors, light/dark mode) is a workspace-level theme — separate from view-level styling. Pair with persona-based workspaces to apply per-role. |

### Authoring views & guides

| Topic | Description |
| --- | --- |
| [Views — UI templates overview](https://docs.aws.amazon.com/connect/latest/adminguide/view-resources-sg.html) | What a View resource is: a template + input schema + actions, used by the Show view block. Recommends the **Set JSON** option in the block for the cleanest data mapping, and notes that every flow namespace (including `$.External`) is reachable from the view. |
| [AWS-managed views](https://docs.aws.amazon.com/connect/latest/adminguide/view-resources-managed-view.html) | Per-template configuration reference for the Detail, Form, List, Cards, and Confirmation managed views: required vs optional sections, supported components (AttributeBar, Sections), and what each renders. |
| [Customer-managed views](https://docs.aws.amazon.com/connect/latest/adminguide/view-resources-custom-view.html) | API-driven authoring: example `aws connect create-view` call, the `Template` + `Actions` `view-content.json` shape, CloudFormation/CloudTrail/tagging support, and a worked Body example with Container + Card + Button. |
| [UI component library](https://docs.aws.amazon.com/connect/latest/adminguide/user-interface-component-library-sg.html) | How the UI builder's Library tab maps to the Storybook component reference. Calls out Containers as the layout primitive and Form as the special container that submits captured fields back to the flow. |
| [Customize panel — layout, colors, dynamic data](https://docs.aws.amazon.com/connect/latest/adminguide/no-code-ui-builder-customize-panel.html) | The right-hand Customize panel toggles between global view settings (12-column flexbox, primary/secondary/neutral colors, alignment) and per-component settings. |
| [Dynamic fields](https://docs.aws.amazon.com/connect/latest/adminguide/no-code-ui-builder-properties-dynamic-fields.html) | Click the lightning-bolt icon on any property to bind it to runtime data passed in via Show view. The most common targets are `Value` on display components and `DefaultValue` on form inputs. Sample Data appears once a field is dynamic. |
| [Conditional UI](https://docs.aws.amazon.com/connect/latest/adminguide/ui-conditions-on-views.html) | Set Visibility, DefaultValue, Required, Disable, or Options on one component based on the value of another component in the same view. Authored from the **UI conditions** tab in the Customize panel. |
| [HTML and JSX in Show view](https://docs.aws.amazon.com/connect/latest/adminguide/customize-views-jsx-sg.html) | Pass HTML or JSX template strings into the Show view block (via **Set JSON**) to render rich content inside a managed view's `Sections.TemplateString` — useful for ordered lists, embedded markup, or inline scripts. |
| [Branch on view actions](https://docs.aws.amazon.com/connect/latest/adminguide/no-code-ui-builder-setting-actions-in-flows.html) | Each button's `Action` value becomes a branch on the Show view block in the flow. Define one Action per button and the flow gains one labelled exit per button. |

### Integration patterns & use cases

| Topic | Description |
| --- | --- |
| [Invoke a guide at the start of a contact](https://docs.aws.amazon.com/connect/latest/adminguide/how-to-invoke-a-flow-sg.html) | Use a `Set event flow` block with the `DefaultFlowForAgentUI` event hook to dynamically pick which guide flow runs when the contact is offered — branch on IVR digits, queue, or attributes with `Check attribute` first. |
| [Display contact attributes (screen pop)](https://docs.aws.amazon.com/connect/latest/adminguide/display-contact-attributes-sg.html) | Canonical screen-pop pattern: a Detail managed view with `Sections` + an optional `AttributeBar`, populated from contact attributes through Show view's Set JSON. |
| [Disposition codes (ACW form)](https://docs.aws.amazon.com/connect/latest/adminguide/disposition-codes-sg.html) | End-of-contact wrap-up: a flow with one Show view (Form view) plus one `Set contact attributes` block, surfaced by setting the `DisconnectFlowForAgentUI` attribute before the contact ends. Optional Lambda block forwards the response to a CRM. |
| [PII redaction in guides](https://docs.aws.amazon.com/connect/latest/adminguide/step-by-step-guides-pii-redaction.html) | Anything captured in a guide lands in the contact record transcript by default. Drop in a `Set recording and analytics behavior` block and enable Contact Lens redaction to keep PII out. |
| [Integrate views with Connect resources](https://docs.aws.amazon.com/connect/latest/adminguide/integrate-views-with-connect-resources.html) | View-level **Tools** poll a Connect resource (e.g. a Flow module) on a refresh interval. Reference outputs in component props with `$.#[IntegrationName].[ReferenceObject]`. Best with one tab open per agent — multi-tab amplifies polling. |
| [App integration (third-party screen pop)](https://docs.aws.amazon.com/connect/latest/adminguide/no-code-ui-builder-app-integration.html) | Three options for embedding third-party apps: the `Application` component (in-Guide), the App Launch component (separate workspace tab with auto-open), or a `Link` component with auto-open. App name must match the AWS Management Console registration exactly. |
| [Step-by-step guides in chat](https://docs.aws.amazon.com/connect/latest/adminguide/step-by-step-guides-chat.html) | Reuse an agent guide as a customer self-service experience: the same Show view block runs in the chat flow and renders inside a hosted Amazon Connect chat widget before transferring to an agent. |
| [Persona-based workspace pages](https://docs.aws.amazon.com/connect/latest/adminguide/use-views-to-create-persona-based-workspace-pages.html) | Use Views as single-step pages inside a persona-based Workspace (not a guide). Compatible components include Alert, Carousel, Containers, and Data Table. Input data flows in through the page configuration wizard. |
| [Guides in manager workspaces](https://docs.aws.amazon.com/connect/latest/adminguide/use-guides-in-manager-workspace.html) | Embed a guide inside a static workspace view via the `Connect Application` component (set Application Namespace = Guide, ContactFlowId = the guide flow). Begin/Restart controls launch the background chat contact. Cannot be nested in another guide-driven view. |
